location ^~ /tourism/api/vworld/tiles/ {
    limit_except GET { deny all; }
    client_max_body_size 2k;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 3s;
    proxy_read_timeout 25s;
    proxy_pass http://127.0.0.1:18081/vworld/tiles/;
}

location = /tourism/api/vworld/geocode {
    limit_except POST { deny all; }
    limit_req zone=tourism_ai burst=2 nodelay;
    client_max_body_size 2k;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 3s;
    proxy_read_timeout 25s;
    proxy_pass http://127.0.0.1:18081/vworld/geocode;
}

location = /tourism/api/regulations/point {
    limit_except GET { deny all; }
    limit_req zone=tourism_ai burst=4 nodelay;
    client_max_body_size 2k;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 3s;
    proxy_read_timeout 25s;
    proxy_pass http://127.0.0.1:18081/regulations/point;
}

location = /tourism/api/regulations/insight {
    limit_except POST { deny all; }
    limit_req zone=tourism_ai burst=2 nodelay;
    client_max_body_size 2k;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 3s;
    proxy_read_timeout 110s;
    proxy_pass http://127.0.0.1:18081/regulations/insight;
}
